# Optional configuration for doc-extractor.
# Copy to .env and adjust as needed.

# Default directory the CLI batch mode reads from when --batch is given without a path.
DOC_EXTRACTOR_INPUT_DIR=./data/inputs

# Default directory the CLI writes IR JSON to when --out is given without a path.
DOC_EXTRACTOR_OUTPUT_DIR=./data/outputs
